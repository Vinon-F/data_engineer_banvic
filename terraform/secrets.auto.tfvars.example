# Copie este arquivo para "secrets.auto.tfvars" e preencha com valores reais.
# secrets.auto.tfvars NUNCA deve ser commitado (adicione ao .gitignore).

minio_root_password    = "troque-esta-senha"
airflow_admin_password = "troque-esta-senha"

# Gere com: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
airflow_fernet_key = "troque-esta-fernet-key"
